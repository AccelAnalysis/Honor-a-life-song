# PR Preview Verification

This temporary change exists only to verify that the automatic pull-request preview pipeline builds, publishes, comments the preview URL, and cleans up after the pull request closes.
