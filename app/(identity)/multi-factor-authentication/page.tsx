import { IdentityLifecyclePlaceholder } from "@/components/identity-lifecycle-placeholder";

export default function MultiFactorAuthenticationPage() {
  return <IdentityLifecyclePlaceholder title="Verify It’s You" description="An extra verification step helps protect private songs, stories, family contributions, and program information connected to your account." />;
}
